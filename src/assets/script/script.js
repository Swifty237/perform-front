if (document.querySelector('.section-select')) {
    document.addEventListener("DOMContentLoaded", () => {
        const navHeight = document.getElementById('mainNav').offsetHeight;
        const header = document.querySelector('.section-select');
    });
}