export const environment = {
    production: true,
    backNodeUrl: 'https://perform-back-nodejs.vercel.app',
    backSpringUrl: "https://perfmma.backspring.link/"
};

//ng build --configuration=production